#include <iostream>
using namespace std;
int main()
{
    int[10], n, i, LOC;
    cout << "Enter the size of array";
    cin >> n
    cout<< "Enter the elememnt";
    for (i = 0; i < n : i++)
    {
        cin >> a[i];
    }
    cout << "Enter location";
    cin >> LOC;
    for (i = LOC - 1; i < n - 1; i++)
    {
        a[i] = a[i + 1];
        cout << "The find the array after deletion is\n";
        for (i = 0; i < n - 1; i++)
        {
            cout << a[i];
        }
    
    }
    return 0
}